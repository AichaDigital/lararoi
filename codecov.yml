coverage:
  status:
    project:
      default:
        target: auto        # never drop below the current base
        threshold: 0.3%
    patch:
      default:
        target: 90%         # new code must be >=90% covered
comment:
  layout: "reach, diff, files"
  require_changes: true
